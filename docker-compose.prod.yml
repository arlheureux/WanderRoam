# docker-compose.yml - Production with Traefik reverse proxy
# Run with: docker-compose up -d
# Requires: Domain name and ports 80, 443 forwarded to server

services:
  traefik:
    image: traefik:v3.0
    container_name: wanderroam-traefik
    command:
      - "--api.insecure=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--certificatesresolvers.letsencrypt.acme.email=admin@example.com"
      - "--certificatesresolvers.letsencrypt.acme.storage=/letsencrypt/acme.json"
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge=true"
      - "--certificatesresolvers.letsencrypt.acme.httpchallenge.entrypoint=web"
    ports:
      - "80:80"
      - "443:443"
      - "8080:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - traefik_certs:/letsencrypt
    restart: on-failure

  redis:
    image: redis:7-alpine
    container_name: wanderroam-redis
    networks:
      - wanderroam-internal
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 3
    restart: on-failure
    volumes:
      - redis_data:/data

  postgres:
    image: postgres:15-alpine
    container_name: wanderroam-db
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - wanderroam-internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: on-failure

  backend:
    image: arlheureux/wanderroam-backend:stable
    container_name: wanderroam-backend
    env_file:
      - .env
    environment:
      - BROUTER_URL=http://wanderroam-brouter:17777/brouter
      - REDIS_URL=redis://wanderroam-redis:6379
    volumes:
      - upload_data:/app/uploads
    networks:
      - wanderroam-internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend.rule=PathPrefix(`/api`)"
      - "traefik.http.routers.backend.entrypoints=websecure"
      - "traefik.http.routers.backend.tls=true"
      - "traefik.http.services.backend.loadbalancer.server.port=5000"
    depends_on:
      postgres:
        condition: service_healthy
      brouter:
        condition: service_started
      redis:
        condition: service_healthy
    restart: unless-stopped

  frontend:
    image: arlheureux/wanderroam-frontend:stable
    container_name: wanderroam-frontend
    networks:
      - wanderroam-internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=PathPrefix(`/`)"
      - "traefik.http.routers.frontend.entrypoints=websecure"
      - "traefik.http.routers.frontend.tls=true"
      - "traefik.http.services.frontend.loadbalancer.server.port=80"
    depends_on:
      - backend
    restart: unless-stopped

  admin:
    image: arlheureux/wanderroam-admin:stable
    container_name: wanderroam-admin
    networks:
      - wanderroam-internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.admin.rule=PathPrefix(`/admin`)"
      - "traefik.http.routers.admin.entrypoints=websecure"
      - "traefik.http.routers.admin.tls=true"
      - "traefik.http.services.admin.loadbalancer.server.port=80"
    depends_on:
      - backend
    restart: unless-stopped

  brouter:
    image: arlheureux/wanderroam-brouter:stable
    container_name: wanderroam-brouter
    networks:
      - wanderroam-internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.brouter.rule=PathPrefix(`/brouter`)"
      - "traefik.http.routers.brouter.entrypoints=websecure"
      - "traefik.http.routers.brouter.tls=true"
      - "traefik.http.services.brouter.loadbalancer.server.port=17777"
    environment:
      - COUNTRIES=france,denmark
    volumes:
      - brouter_data:/segments4
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:17777/"]
      interval: 10s
      timeout: 5s
      retries: 3
    restart: on-failure

volumes:
  postgres_data:
  upload_data:
  brouter_data:
  redis_data:
  traefik_certs:

networks:
  wanderroam-internal:
    name: wanderroam-internal
